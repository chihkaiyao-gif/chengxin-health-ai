import { AppShell } from "@/components/app-shell";
import { TrainingLogWorkspace } from "@/components/training-log-workspace";

export default function TrainingPage() {
  return (
    <AppShell>
      <TrainingLogWorkspace />
    </AppShell>
  );
}
